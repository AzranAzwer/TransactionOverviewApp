import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    height: 55,
    width: 320,
    borderRadius: 15,
    padding: 10,
    borderColor: 'grey',
    borderWidth: 2,
  },
});
